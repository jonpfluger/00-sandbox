<div class="menu-container">
  <div class="card-header col-md-10">
  <h2>
    {{{dish_name}}}
  </h2>
  </div>
  <div>
    <p>{{{description}}}</p>

    {{#if has_nuts}}
      <p>
        <strong>
          ⚠ THIS DISH CONTAINS NUTS!
        </strong>
      </p>
    {{else}}
      <p>No nuts detected.</p>
    {{/if}}
  </div>
</div>